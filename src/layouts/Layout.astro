---
import "../styles/global.css";
import { ClientRouter } from "astro:transitions";

interface Props {
  class?: string;
  title?: string;
  description?: string;
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <meta name="generator" content={Astro.generator} />
    <!-- seo -->
    <meta
      name="description"
      content="Portfolio of Guru Sharan, a Full Stack Developer specializing in React, Node.js, and scalable web applications."
    />
    <meta name="keywords" content="Guru Sharan, Full Stack Developer, React, Node.js, MongoDB, Portfolio" />
    <meta name="author" content="Guru Sharan Kumar Priye" />

    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://gurusharan.dev/" />
    <meta property="og:title" content="GuruSharan.dev | Full Stack Developer Portfolio" />
    <meta
      property="og:description"
      content="Portfolio of Guru Sharan, a Full Stack Developer specializing in React, Node.js, and scalable web applications."
    />
    <meta property="og:image" content="/og-logo.png" />
    <ClientRouter />

    <title>GuruSharan.dev | Full Stack Developer</title>
  </head>
  <body class={Astro.props.class}>
    <slot />

    <!-- Intersection Observer para animaciones de reveal -->
    <script is:inline>
      let observerInstance = null;

      function initializeObserver() {
        // Limpiar observer anterior si existe
        if (observerInstance) {
          document.querySelectorAll(".item-revelar").forEach((el) => {
            observerInstance.unobserve(el);
          });
        }

        const observerConfig = { threshold: 0.1 };
        observerInstance = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("animate-in");
              observerInstance.unobserve(entry.target);
            }
          });
        }, observerConfig);

        document.querySelectorAll(".item-revelar").forEach((el) => {
          observerInstance.observe(el);
        });
      }

      initializeObserver();

      document.addEventListener("astro:page-load", () => {
        initializeObserver();
      });
    </script>

    <!-- Flowbite - Importado desde npm -->
    <script>
      import * as flowbite from "flowbite";

      // Inicializar Flowbite en carga inicial
      flowbite.initFlowbite();

      // Reinicializar Flowbite después de navegación con ClientRouter
      document.addEventListener("astro:page-load", () => {
        flowbite.initFlowbite();
      });
    </script>
  </body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }
</style>
